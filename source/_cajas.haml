%h2#sensible Sensible

.row-fluid
  .caja
    .span2
      = image_tag(lorem.image('200x200'))
    .span10
      :markdown
        Sensores de **temperatura** y **humedad** para lograr un ambiente
        óptimo.

%h2#puntual Puntual

.row-fluid
  .caja
    .span2.pull-right
      = image_tag(lorem.image('200x200'))
    .span10
      :markdown
        **Reloj** y **calendario** emulan el fotoperiodo natural y te recuerdan
        cuándo fertilizar.

%h2#extensible Extensible

.row-fluid
  .caja
    .span2
      = image_tag(lorem.image('200x200'))
    .span10
      :markdown
        Extensible con la línea de **sensores** y **actuadores** de Cultibot:
        automatizá la altura de la lámpara, la fertilización, y más...

%h2#nacional Nacional

.row-fluid
  .caja
    .span2.pull-right
      = image_tag(lorem.image('200x200'))
    .span10
      :markdown
        Cultibot es **Industria Nacional** y de bajo costo. Cuesta menos y es
        más completo que controladores importados.
